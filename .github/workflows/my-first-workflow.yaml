name: Build and deploy
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: exit 1  # Simulamos un error en la construcción

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying"
